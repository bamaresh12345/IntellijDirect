package GIT;

public class GIT_CHECKOUT {

    /*
        1.  git checkout -b <branch_name> --> create a new branch and switch to it
        2.  git checkout <branch_name> --> switch to an existing branch
        3.  git checkout - --> switch to the previous branch
        4.  git checkout . --> discard all changes in the working directory
        5.  git checkout -- <file_name> --> discard changes in a specific file
        6.  git checkout <commit_hash askdhjf;lkasd23> --> switch to a specific commit (detached HEAD state)
        7.  git checkout -b <new_branch_name> <base_branch_name> --> create a new branch based on another branch
        8.  git checkout --track <remote_branch_name> --> create a local branch that tracks a remote branch


     */
}
